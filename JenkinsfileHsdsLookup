pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'pip install poetry'
                sh 'poetry install'
            }
        }

        stage('Build') {
            steps {
                withCredentials([
                    string(credentialsId: 'hsds-data', variable: 'PG_DATABASE', binding: 'PG_DATABASE'),
                    string(credentialsId: 'hsds-data', variable: 'PG_USER', binding: 'PG_USER'),
                    string(credentialsId: 'hsds-data', variable: 'PG_PASSWORD', binding: 'PG_PASSWORD'),
                    string(credentialsId: 'hsds-data', variable: 'PG_HOST', binding: 'PG_HOST'),
                    string(credentialsId: 'hsds-data', variable: 'PG_PORT', binding: 'PG_PORT')
                ]) {
                    script {
                        sh 'poetry run python highstreets/hsdsprocess/hsds_bid_hs_tc_lookup.py'
                    }
                }
            }
        }
    }
}
