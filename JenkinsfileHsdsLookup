pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'pip install poetry'
                sh 'poetry install'
            }
        }

        stage('Build') {
            steps {
                withCredentials([
                    file(credentialsId: 'hsds-data', variable: 'PG_DATABASE'),
                    file(credentialsId: 'hsds-data', variable: 'PG_USER'),
                    file(credentialsId: 'hsds-data', variable: 'PG_PASSWORD'),
                    file(credentialsId: 'hsds-data', variable: 'PG_HOST'),
                    file(credentialsId: 'hsds-data', variable: 'PG_PORT')
                ]) {
                    script {
                        sh 'poetry run python highstreets/hsdsprocess/hsds_bid_hs_tc_lookup.py'
                    }
                }
            }
        }
    }
}
